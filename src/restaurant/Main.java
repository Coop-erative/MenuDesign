package restaurant;

public class Main {

    public static void main(String[] args) {
	    /* Menu Organization:
	         ArrayList<Hashmap<String key, ArrayList<String> ()> currentMenu
	         ArrayList<menuItem menuItems> ;
	         name
	         category
	         price
	         createdDate
	         newOrOld

	         **have two  that are overloaded. one without new or not and one with.
	         **the update does not seem to be something to print out?
	     */

        // Run Program:
        try {

            do {

                switch () {

                    case 1:
                        break;
                    case 2:

                }
            } while ();

        } catch () {

        } finally {

        }

    }//end main method
}
